Parity fixtures: verbatim copies of `output/<ETF>_prices.csv` produced by the
legacy Streamlit pipeline (data as of 2026-06-26). Used ONLY to assert the new
implementation reproduces the legacy numbers on identical input. Never loaded
at runtime.
